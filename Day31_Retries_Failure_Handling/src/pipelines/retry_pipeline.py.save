attempt = 0


def unstable_task():
    global attempt

    attempt += 1

    print(f"Attempt Number: {attempt}")

    # Fail first 2 times
    if attempt < 3:
        raise Exception("Simulated Fa		ilure!")

    print("Task succeeded on retry!")
