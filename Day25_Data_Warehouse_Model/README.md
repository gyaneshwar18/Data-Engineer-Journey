# Description

This project introduces data warehouse modeling using the Star Schema.

Instead of storing all data in a single table, the data is organized into:

fact tables (store measurable events)

dimension tables (store descriptive attributes)

# This structure improves:

query performance

data organization

analytical capabilities