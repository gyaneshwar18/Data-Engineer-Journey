# Description

This project introduces incremental data loading, a critical concept in production data pipelines.

Instead of reloading all data every time, only new or updated records are processed.

# This is done by tracking metadata such as:

last processed ID

last processed timestamp